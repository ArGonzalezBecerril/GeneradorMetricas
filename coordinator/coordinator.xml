<coordinator-app end="${endTime}" frequency="${frequency}" name="${appName}" start="${startTime}" timezone="${timezone}" xmlns="uri:oozie:coordinator:0.2">
   <action>
      <workflow>
         <app-path>${workflowPath}</app-path>
      </workflow>
      </workflow>
   </action>
</coordinator-app>